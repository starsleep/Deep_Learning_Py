//비선형 함수

선형 함수
- 출력이 입력의 상수배 만큼 변하는 함수를 선형함수라고 한다.

비선형 함수
- 선형이 아닌 함수로 직선 1개로는 표현할수 없는 함수를 말한다.

신경망에서 비선형 함수를 사용하는 이유?
- 선형함수를 사용하면 신경망의 층을 깊게하는 이유가 사라진다.

  ex) h(x) = cx 인 함수를 3층 신경망으로 쌓았을 경우
  - h(h(h(x))) = c*c*c*x 로 표현할수 있다.

  -> 따라서 3층 신경망을 하나의 층으로 표현할수 있기 때문에 활성화 함수로 비선형 함수를 사용해야 한다.
